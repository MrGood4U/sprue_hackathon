# Backend Instructions

On 2026-09-11, the user activated the generic live hosted-runtime profile, superseding the earlier read-only build-preflight and materialized-serving defaults. A successful explicit Builder build must persist an immutable version containing the canonical DAG, static provider query documents, compiler identity, and hashes. Hosted data routes are owner-scoped, use the product UUID as the canonical final segment with an optional owner-scoped alias, and require a Sprue-issued API key on every request. Load and verify the immutable plan without recompiling it per request. Every accepted request must issue fresh bounded The Graph queries and execute the fixed DAG; cache compiled plans, schemas, and query metadata only, never provider rows or final results. Private-deployment export must contain the same bounded interpreter and immutable plan, without creator credentials or arbitrary-code hooks.

On 2026-09-08, the user approved direct Dashboard-to-Agent product creation. `POST /products` may accept an empty `originalIntent` only to create the new draft; the first accepted non-empty Agent message initializes `data_products.original_intent` atomically and advances `lock_version`, while later messages must not overwrite that initial objective. No model or Graph request belongs in product creation.

On 2026-09-09, the user superseded the earlier 120-second planning limit after a real long-running request outlived the browser. Treat `AGENT_TIMEOUT_MS` as the bounded timeout for each external model or Graph step, default it to 600 seconds, and permit configuration up to 1,800 seconds. Treat `AGENT_RUN_TIMEOUT_MS` as the fixed whole-run deadline, default it to 3,600 seconds, and permit configuration up to 7,200 seconds. The run deadline must be at least the step timeout. Do not add automatic paid retries, and emit only structured failure category, HTTP status, and duration diagnostics. Never log the model URL, API key, prompt, provider body, user intent, workspace, product, session, or user identifiers.

On 2026-09-09, the user approved explicit cancellation of an in-flight Agent planning command. The authenticated cancellation route must target the exact workspace/session command, mark cancellation requested, abort the shared model/Graph run signal, and let the original handler persist a terminal cancelled command and sanitized result. A browser-side fetch abort is not cancellation. The current synchronous implementation may support only runs owned by the same API process; missing process-local execution state must fail explicitly rather than claim the command stopped.

On 2026-09-09, the user approved splitting live source feasibility into a compact entity-selection model stage and a later field-binding/composition stage. The selector may return only supplied source and query-entity references plus bounded explanation metadata. The Harness must validate those references and expand only the selected entities' fields from trusted discovery evidence before the later model call. Keep one shared repair allowance inside the four-call command ceiling, and use a 16,384-token output ceiling for each remote planning call.

On 2026-09-10, the user approved an optional no-cache embedding retrieval step before compact entity selection. When enabled through API-only environment configuration, embed bounded source-need text and actual inspected entity/field documents, compute similarity in the Harness, retain fair candidate coverage, and give the selector only compact references and advisory scores. Do not cache vectors or responses, let embeddings choose a source, expose full field arrays to the selector, or weaken deterministic network, candidate, schema, operator, and resource validation. If configured retrieval fails, report it explicitly; when disabled, retain the deterministic compatibility ranking.

On 2026-09-10, the user approved hierarchical semantic retrieval for source planning. First embed bounded whole-entity documents and let the entity-selector model choose one inspected entity per source need. Only after that choice, embed every field of each selected entity as its own bounded document, compare all field vectors locally against every declared semantic field requirement, and expose the union of per-requirement ranked alternatives to the source-feasibility model. Do not apply a fixed global field-count prefilter before field embedding; retained evidence should grow with the number of semantic requirements. Keep the complete selected-entity schema in trusted Harness memory, permit model bindings only to presented paths, and validate those paths again against the complete inspected schema. When embeddings are disabled, retain the bounded deterministic field-ranking fallback. Do not hardcode protocols, assets, networks, query entities, or provider field names.

On 2026-09-10, the user approved the version-2 generic Filter predicate. It contains one bounded top-level AND/OR group and typed field conditions, references only scalar fields from the direct predecessor output schema, and preserves exact integer/decimal strings. Its residual conditions execute on fetched Sprue rows; a complete stable predicate may instead be moved into an Agent-authored, schema-validated GraphQL query under the newer pushdown decision below. Keep field/operator validation deterministic and field-name agnostic. Missing or type-changed upstream references must fail visibly rather than being renamed, deleted, or coerced. JSON, object and list filtering remains unsupported until a reviewed flatten/explode operator exists.

On 2026-09-10, the user approved the version-1 Sort / Top K operator. It contains one to eight prioritized scalar field keys, explicit asc/desc direction, independent first/last null placement, and a nullable bounded K from 1 through 10,000. It executes on already-fetched Sprue rows, preserves the input schema, and uses upstream row order as the deterministic final tie-breaker. A configured K may use bounded heap selection but must produce exactly the same ordered prefix as a complete stable sort. Keep schema validation field-name agnostic, reject JSON/object/list sorting, and preserve invalid upstream references visibly.

On 2026-09-10, the user approved the bounded generic Map transform set. Support explicit integer, decimal, ISO timestamp, epoch-seconds, and epoch-milliseconds conversions; trim/lower/upper and bounded concatenation; compatible-type coalesce; and abs/half-even round/floor/ceil. Execute on already-fetched rows, preserve exact integer/decimal strings, reject fractional to-integer casts unless an explicit rounding operator is present, and never guess epoch units or provider field semantics. Keep Harness inference, deterministic execution, and frontend schema inference aligned.

All repository text, code comments, test names, migration notes, and commit messages must be written in English. Communication with the user may be in Chinese.

Use the approved root `data-model.md` as the persistence baseline. Do not invent durable fields in endpoint code; update the reviewed model first when a missing concept is discovered.

The current persistence baseline is version 1.13. Sprue `users.id` is application-owned; provider subjects belong only in `auth_identities`, which permits multiple explicit bindings per user. Private creator resources are workspace-scoped, and creator-attributed records must name a user from the same workspace. Model and Graph API keys must be encrypted with the server-only versioned keyring before SQL and never returned. Graph credentials may be validated through the bounded server adapter, selected as the single workspace default only after successful validation, and revoked with ciphertext destruction; exact product/source use remains pinned separately. Authenticated bootstrap idempotently provisions and binds one user-owned Privy Ethereum wallet, but that wallet has no Sprue signer grant or spending authority. Explicit Hedera testnet activation may fund only that server-selected bound address, records an idempotent command, and reconciles through Mirror Node. A hollow account remains pending. A complete account at the same bound Privy EVM address is accepted as verified creator control with HBAR spend capability because the creator-confirmed live Privy EVM transaction demonstrated signing and fee spend on this testnet path. This enables only creator-confirmed browser-to-Privy direct testnet withdrawals under fixed reviewed profiles. It does not enable backend transfer commands, durable withdrawal reconciliation, delegated spending, native Hedera x402 signing, or mainnet behavior. Account linking is not implemented and must never use email or wallet heuristics. Read `database.md` before database work. SQL migrations are the authoritative schema; split Drizzle files are query mappings, not a complete migration-generation source. Do not use schema push or edit already applied migration history. Database tests use isolated PGlite only; native PostgreSQL and concurrency verification remain separate gates. Do not infer payment authority from a wallet record, balance observation, seeded public network metadata, faucet disbursement, or structural test fixture.

Keep API and worker entry points separate while sharing domain modules. Keep provider SDKs behind adapters, secrets out of the frontend and database, and all paid or retryable operations idempotent and reconcilable.

Before changing The Graph, Privy, or Hedera behavior, read the matching active file under `sponsor/`. Do not claim a provider capability without implementation evidence.

On 2026-09-11, the user required Builder Source-modal search and direct identifier verification to use the live restricted Graph MCP adapter. Keep both operations creator-authenticated and workspace-scoped, resolve only the selected active Graph credential on the server, bound search/result/schema sizes, accept only logical Subgraph ID, deployment ID, or manifest IPFS CID references, and return inspected query entities with exact provider field paths. These metadata/schema reads may create editable planning evidence only; they must not persist an admitted source snapshot, execute an arbitrary GraphQL document, deploy a Subgraph, or imply runtime coverage/access approval.

On 2026-09-11, the user required Map output units to remain generic semantic metadata. A version-2 Map field may declare `unit` as a bounded string or null; null inherits an already known expression unit, while a string may annotate a provider value whose inspected schema lacks unit metadata. This never converts a value. Reject conflicts with known expression units, require Agent annotations to be supported by the mapped semantic source requirements, and never infer a unit from output names, provider field paths, protocols, assets, networks, or provider conventions. Keep Harness inference, structured DAG compilation, frontend propagation, and legacy projection aligned.

On 2026-09-11, the user required the Builder's current layout-free Structured DAG to be compiled by the backend when `Run backend build` is clicked. Keep `build-preflight` creator-authenticated, workspace/product-scoped, bounded, and deterministic. Validate graph topology, one Output, acyclicity, full reachability, exact operator versions and ports, exclusive Source-to-project-Map normalization, operator configurations, predecessor-schema propagation, multi-input compatibility, and the declared output contract. A failed compilation remains read-only and returns bounded safe issue details. A passed compilation now admits the exact sources and persists or reuses the matching immutable ready version as required by the newer live-runtime decision above; it still does not execute result rows, reserve funds, or activate a deployment. The browser owns the pending spinner, failure modal, and same-product API navigation after a pass.
On 2026-09-11, the user confirmed a strict Source-to-project-Map normalization boundary in both the full Agent composition and compiled Builder DAG. Persist exact inspected provider field paths and types on Source, require explicit bindings, and reject invented aliases or hidden type changes. The Source adapter must preserve the raw nested provider shape. Every provider-to-semantic rename, flattening, conversion, unit annotation, and derivation remains visible and executable in the immediate project-mode Map; that Map must never disappear as query pushdown.

On 2026-09-11, the user moved bounded GraphQL pushdown into the live Agent planning stage and required it to be a real semantic rewrite rather than duplicate execution. Each feasible Agent-selected Source carries one inspectable `SprueLiveSource` GraphQL document plus bounded cursor limits and actual Filter/Sort node-role evidence. Validate the complete semantic composition first, then create the residual Builder DAG: a Filter or Sort may be listed only when its complete stable semantics are represented by the query, and that unary node is removed with edges rewired. Field selection is not Map pushdown: raw provider paths stay on Source and the mandatory Map remains. Deterministically reject additional roots, fragments, aliases, directives, arbitrary variables, uninspected fields, mismatched cursor types, unrelated node roles, or filter metadata that does not match an added `where` predicate. The Builder compiler validates the Agent-authored document against the freshly inspected provider schema and persists that exact text in the immutable live plan; it may generate its bounded fallback query only for manually added or historical sources without an Agent query plan. Runtime callers never supply or edit the stored query.

On 2026-09-11, the user required a Source-level row `limit` with a 1,000-row default for new nodes and an editable range of 1 through 10,000. Validate and freeze this limit during Builder compilation, cap it by the Agent query plan's independent safety ceiling, and treat reaching the requested row limit as successful bounded execution. Continue to fail when the independent request cap is exhausted while a full final page indicates more provider rows remain. Keep hosted and private-runner semantics identical.

On 2026-09-11, the user raised Sprue's default The Graph cursor page size from 500 to the provider-supported maximum of 1,000 rows. New Agent-authored and compiler-fallback Source plans use 1,000 rows per request, while the editable Source row `limit` remains the separate total-result bound. Keep smaller explicitly authored page sizes valid for exceptional query-complexity cases, and never raise either the total-row safety ceiling or request ceiling implicitly.

On 2026-09-10, the user required Output to accept exactly one `rows` predecessor and to have no ordering behavior. Current flexible plans use Output version 3 with `{fields}` only. Every non-empty semantic result ordering must be implemented by a version-1 Sort / Top K operator immediately before Output with identical field priority and directions; enforce this deterministically in addition to prompting the model. Preserve incoming order at Output and never hide ranking or Top K semantics in publication configuration.
