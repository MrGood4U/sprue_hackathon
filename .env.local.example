# Prefer scripts/local.ps1 init, which creates a random local-only password.
# This file is for the complete sprue-local Compose project, not cloud deployment.
POSTGRES_PASSWORD=replace-with-64-hex-characters
POSTGRES_PORT=15432
API_PORT=3001
FRONTEND_PORT=4173
